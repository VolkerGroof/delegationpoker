# Where the PartyKit server lives. Locally this is the dev server PartyKit
# runs on `npm run dev:party`. In production, after `npm run deploy:party`,
# replace with the printed URL host, e.g. `delegationpoker.<user>.partykit.dev`.
VITE_PARTYKIT_HOST=localhost:1999
